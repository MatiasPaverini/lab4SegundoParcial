import { ClassSizeDirective } from './class-size.directive';

describe('ClassSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassSizeDirective();
    expect(directive).toBeTruthy();
  });
});
